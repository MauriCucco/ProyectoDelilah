const express = require("express");
const pedido = express.Router();

//subrutas de /pedidos
//pedido.post("/", subirPedido);
//pedido.get("/estado/:IdPedido", obtenerEstadoPedido);
//pedido.get("/favoritos/:IdCliente", obtenerPedidosUsuario);

module.exports = pedido;